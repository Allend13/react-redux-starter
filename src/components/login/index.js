import React, { PropTypes } from 'react'
import './login.less'

const Login = () =>
  <div className="login">
    Login
  </div>

Login.propTypes = {
  user: PropTypes.object,
}

export default Login
